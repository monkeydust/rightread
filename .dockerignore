node_modules
.next
.git
tmp
scratch
tests
# Local database and its backups must never end up baked into the image —
# the server's own volume is the source of truth in production.
prisma/dev.db
prisma/*.db-journal
prisma/backups
.env*
extension
